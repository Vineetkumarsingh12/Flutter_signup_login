

import 'dart:ui';

import 'package:flutter/material.dart';

const tPrimaryColor=Color(0xFFFFE400);
const tSecondaryColor=Color(0xFF272727);
const tAccentColor=Color(0xFF001BFF);

const tCardBgColor=Color(0xFFF7F6F1);
const TWhiteColor=Color(0xffffffff);
const tDarkColor=Color(0xFF000000);


// Define standard colors
const Color primaryColor = Colors.blue; // Primary color for the app
const Color accentColor = Colors.grey; // Accent color for hints and additional text
const Color errorColor = Colors.red; // Error color for validation messages
const Color buttonTextColor = Colors.white; // Button text color